version: '3.8'

services:
  app:
    build: .
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DATABASE_URL=${DATABASE_URL}
      - PYTHONPATH=/app/src
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true